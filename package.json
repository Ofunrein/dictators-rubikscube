{
  "name": "rubiks-workspace",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "start": "node scripts/dev.mjs",
    "dev": "node scripts/dev.mjs",
    "dev:api": "npm --prefix backend/api run serve",
    "dev:frontend:legacy": "npm --prefix frontend-legacy run dev"
  }
}
